package ie.gaelgrounds.app.ui.theme

import androidx.compose.ui.graphics.Color

// Mirrors ios/GaelGrounds/Utilities/Theme.swift -- same palette, both platforms.
val BrandGreen = Color(0xFF0B3D2E)
val BrandGreenLight = Color(0xFF14603F)
val BrandGold = Color(0xFFD9A441)
val BrandLive = Color(0xFFD64545)

val BrandCanvasLight = Color(0xFFF6F8F3)
val BrandCanvasDark = Color(0xFF09130F)

val BrandSurfaceLight = Color(0xFFFBFCF9)
val BrandSurfaceDark = Color(0xFF13201B)

// The slightly-brighter-than-surface fill used behind cards (gaelCard()/
// gaelInsetCard() in Theme.swift) -- distinct from the plain surface tone.
val BrandSurfaceRaisedLight = Color(0xFFFFFFFE)
val BrandSurfaceRaisedDark = Color(0xFF182821)

// Achievement tier tints (AchievementTier.tint in Theme.swift).
val TierBronze = Color(0xFFB87433)
val TierSilver = Color(0xFF8C99A9)
